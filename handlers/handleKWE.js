// handlers/handleKWE.js
export default async function handleKWE({ filename }) {
  console.warn(`⚠️ KWE parser nog niet geïmplementeerd (${filename})`);
  throw new Error('Parser KWE is nog niet geïmplementeerd');
}
